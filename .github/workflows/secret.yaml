name: Echo Secret
on:
  push:
    branches:
      - main

jobs:
  display-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Secret
        run: echo "The secret is ${{ secrets.SECRET }}"
